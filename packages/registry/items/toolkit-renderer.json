{
  "name": "toolkit-renderer",
  "description": "Shared renderer helpers for installable ChatJS tools",
  "dependencies": [
    "react"
  ],
  "files": [
    {
      "path": "cx.ts",
      "type": "lib",
      "target": "_shared/lib/cx.ts",
      "content": "export function cx(...parts: Array<string | false | null | undefined>): string {\n  return parts.filter(Boolean).join(\" \");\n}\n"
    },
    {
      "path": "tool-part.ts",
      "type": "lib",
      "target": "_shared/lib/tool-part.ts",
      "content": "import type { InferUITool, Tool, ToolUIPart } from \"ai\";\n\ntype DistributiveOmit<T, K extends PropertyKey> = T extends unknown\n  ? Omit<T, K>\n  : never;\n\nexport type ToolPartFromTool<NAME extends string, T extends Tool> = ToolUIPart<{\n  [K in NAME]: InferUITool<T>;\n}>;\n\nexport type TypelessToolPartFromTool<\n  NAME extends string,\n  T extends Tool,\n> = DistributiveOmit<ToolPartFromTool<NAME, T>, \"type\">;\n"
    },
    {
      "path": "use-tool-is-compact.ts",
      "type": "hook",
      "target": "_shared/hooks/use-tool-is-compact.ts",
      "content": "\"use client\";\n\nimport { useEffect, useState } from \"react\";\n\nconst COMPACT_BREAKPOINT = 640;\n\nexport function useToolIsCompact(): boolean {\n  const [isCompact, setIsCompact] = useState(false);\n\n  useEffect(() => {\n    const mediaQuery = window.matchMedia(\n      `(max-width: ${COMPACT_BREAKPOINT - 1}px)`\n    );\n\n    const update = () => setIsCompact(mediaQuery.matches);\n    update();\n    mediaQuery.addEventListener(\"change\", update);\n\n    return () => mediaQuery.removeEventListener(\"change\", update);\n  }, []);\n\n  return isCompact;\n}\n"
    }
  ]
}
